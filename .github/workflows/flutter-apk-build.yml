name: Build Flutter APK

on:
  workflow_dispatch: # Allows manual trigger
  push:
    branches:
      - main  # Change this to your working branch if needed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'  # Confirm this is your version

      - name: Install Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: watameal-release-apk
          path: build/app/outputs/flutter-apk/watameal--release.apk
